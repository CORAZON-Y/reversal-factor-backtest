# 题目介绍
单因子检验-根据研报完成以下任务

1.载入数据（基础和市值行业等数据，只载入相关数据提高效率）

2.计算因子和收益率（因子使用数据截至t0日收盘价，因子为（t0日收盘/t-5日收盘）-1，(收益率t2日开盘/t1日开盘)-1）

3.计算收益率时剔除t1日涨跌停和停牌的股票，剔除st股票，剔除上市60天内的股票

4.合并数据（索引：时间，股票代码）

5.因子值标准化（两种方法：1截断后直接zscore 2.rank后）

6.计算因子中性化后的值（后续操作分别对这两种因子做一遍）
a)原始因子值

b)对市值行业中性化（采用论文的残差方法）

7.计算因子IC值

8.因子IC值序列t检验（计算IC均值、标准差等）
9.分层回测（等权或流通市值加权，收益率累加计算，同时计算各类回测评价指标）

a)每个截面上对因子值排序分n层，计算每层收益并累加

b)对每个行业做a操作再累加

10.画出cumulative sum IC和分层回测的图

1.基于预测出的收益率，通过二次规划对因子值最小的100个股票进行个股上下限和行业约束

# 项目文档

本目录集中存放数据字段、研究方法和程序成果说明。项目代码与运行入口位于项目根目录。

## 文档索引

- `数据说明.txt`：原始数据表与字段说明。
- `华泰多因子方法总结.md`：论文多因子、行业中性化、分层回测和组合优化方法整理。
- `程序输出说明.md`：输出目录说明。
- `reports/单因子回测报告.pdf`：单因子回测报告成品。

## 快速运行

以下命令均在项目根目录执行。

当前第二题默认使用 `factor_rank_zscore`，MAD去极值倍数为3，单股权重上限为3%，
组合日方差上限为0.0004。

### 1. 检查环境和测试

```bash
.venv/bin/python -V
.venv/bin/python -m unittest discover -s tests -v
```

### 2. 运行单因子回测

```bash
.venv/bin/python -m factor_analysis --industry-neutral
```

结果写入：

```text
output/单因子回测/
```

### 3. 生成组合优化的预期收益

单因子回测完成后运行：

```bash
.venv/bin/python 二次规划组合优化/expected_return.py
```

内部中间数据生成到：

```text
.cache/二次规划组合优化/expected_returns.parquet
```

### 4. 组合优化短试跑

建议先验证 5 个交易日：

```bash
.venv/bin/python 二次规划组合优化/portfolio_optimization.py \
  --start-date 2010-04-01 \
  --max-dates 5 \
  --output-dir output/二次规划组合优化/试跑
```

运行结束后，终端会显示优化状态统计，并生成一张试跑累计收益图片。

### 5. 全量组合优化

```bash
.venv/bin/python 二次规划组合优化/portfolio_optimization.py
```

结果写入：

```text
output/二次规划组合优化/
```

其中 `portfolio_cumulative_return.png` 是组合的复利累计收益曲线。

## 更多说明

完整参数、因子定义和回测口径以项目根目录的 `README.md` 为准；输出文件解释见
`程序输出说明.md`。
